<div class="container-header bg-secondary text-light mb-5">
        {{-- @if ($back ?? null)
            <a href="{{ route($back) }}"><i class="fa-solid fa-circle-left me-2 fs-3 mb-1"></i></a>
        @endif --}}
      <h2 class="pt-1">{{ $page_title }}</h2>
    </div>
